# VNet details
virtual_network_name = "hk_tiny_vnet"
virtual_network_cidr = "10.0.0.0/21"

# Container network cidr
network_cidr = "10.0.0.0/22"

# We will give the VM Subnet a name that's easily recognized by name
subnet_name = "hk-tinysubnet"

# VM Network subnet prefix. This will allow 32 addresses for VMs and associate network IPs
subnet_prefix = "10.0.4.0/27"

cluster_name = "hk-tinynet"

instance_name = "hk-tinynet"
